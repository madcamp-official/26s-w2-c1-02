# FastAPI 백엔드 환경변수 예시. 복사해서 .env로 사용하세요.
#   cp .env.example .env

# CORS 허용 오리진 (Flutter web dev 서버). 쉼표로 구분.
CORS_ORIGINS=http://localhost:*,http://127.0.0.1:*

# LLM 제공자: mock | gemini  (지금은 mock 고정)
LLM_PROVIDER=mock

# Gemini 연동 시 사용 (지금은 비워둬도 됨)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
